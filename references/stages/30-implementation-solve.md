# 阶段 30：实现与求解

## 目标

让代码和计算服务于模型，生成可回代、可复现的正式结果。

## 要求

- 保留输入校验、参数、环境、随机种子、容差和复现入口。
- 验证程序搜索空间与模型空间一致。
- 输出原始精度值及按题意舍入的结果。
- 把解回代到原方程、硬约束、单位和边界条件。
- 连续最优不得只凭粗网格；必要时用解析条件、局部细化、界或替代算法。
- 通用软件安全、完整性或可信计算加固默认不进入普通数模求解。

## 退出门 SOLVE_PASS

结果已产生，运行可复现，回代通过，没有已知会改变答案的实现错误。随后只进入一次封闭验证。
