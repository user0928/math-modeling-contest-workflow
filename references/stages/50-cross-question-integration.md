# 阶段 50：跨问一致性

## 目标

只检查真正跨问传播的定义、参数、数据版本、单位和结果依赖。

## 检查

- 后问引用的上游版本存在且一致。
- 同名量含义、单位、方向和舍入一致。
- 上游 A 修复会使哪些下游结果失效已明确。
- B 限制在复用时没有被升级为更强结论。

旧审计包保留为历史证据，但不恢复逐阶段双门。C 类记录不触发跨问返工。

## 退出门 INTEGRATION_PASS

所有实际依赖一致，受上游修改影响的结果已做最小必要回归。
