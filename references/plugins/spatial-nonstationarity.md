# 插件：空间非平稳与区域分解

触发：复杂海域/地形/网络/区域，参数随位置变化，等高线或等深线方向改变，局部平面或局部规律明显。

## 强制检查

- 先检验单一全局参数是否合理：查看局部梯度、方向、残差或可行性随位置的变化。
- 比较全局模型与分区/局部模型；分区依据应来自结构变化、误差或约束，而非只为改善结果。
- 每个分区重新估计局部参数和适用方向，不能把全局平均机械复制过去。
- 检查分区边界的衔接、重复覆盖、遗漏、跳变和额外成本。
- 若仍采用单一全局方向或参数，必须给出适用性证据和与局部方案的对比。
- 结论中明确是全局近似、分区最优还是完整空间优化。

